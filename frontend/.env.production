# Vercel editor + copied API URLs must hit the same host n8n uses.
# dash1 is an old deploy (no /api/preview, no with-course route).
VITE_API_URL_PRIMARY=https://certificate-generator2skd.onrender.com
VITE_API_URL=https://certificate-generator2skd.onrender.com
